"""Shared API path constants. Use these for URLs in root response and docs."""

API_PREFIX = "/api"
PATH_HEALTH = f"{API_PREFIX}/health"
PATH_ANALYZE = f"{API_PREFIX}/analyze"
PATH_ANALYZE_CALL = f"{API_PREFIX}/analyze-call"
